import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.discrr',
  appName: 'discrr-app',
  webDir: 'dist/discrr-app',
  bundledWebRuntime: false
};

export default config;
